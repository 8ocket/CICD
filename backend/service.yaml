apiVersion: v1
kind: Service
metadata:
  name: mindlog-be-service
  namespace: default
spec:
  selector:
    app: mindlog-be
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
