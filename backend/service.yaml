apiVersion: v1
kind: Service
metadata:
  name: mindlog-be-service
  namespace: default
spec:
  selector:
    app: mindlog-be
  ports:
    - protocol: TCP
      port: 8080       # 서비스 포트
      targetPort: 8080 # 파드 포트
  type: LoadBalancer
