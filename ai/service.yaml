apiVersion: v1
kind: Service
metadata:
  name: mindlog-ai-service
  namespace: default
spec:
  selector:
    app: mindlog-ai
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
