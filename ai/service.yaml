apiVersion: v1
kind: Service
metadata:
  name: mindlog-ai-service
  namespace: default
spec:
  selector:
    app: mindlog-ai
  ports:
    - protocol: TCP
      port: 8000       # 서비스 포트
      targetPort: 8000 # 파드 포트 (수정!)
  type: ClusterIP
