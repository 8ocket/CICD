name: Deploy

on:
  repository_dispatch:
    types: [deploy]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout CICD repo
        uses: actions/checkout@v4

      - name: Execute deploy.sh
        run: |
          chmod +x ./deploy.sh
          ./deploy.sh

