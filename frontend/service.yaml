apiVersion: v1
kind: Service
metadata:
  name: mindlog-fe-service
  namespace: default
spec:
  selector:
    app: mindlog-fe
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort # 혹은 ALB 연결을 위해 ClusterIP 사용
