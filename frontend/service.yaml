apiVersion: v1
kind: Service
metadata:
  name: mindlog-fe-service
  namespace: default
spec:
  selector:
    app: mindlog-fe
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 3000 # 실제 파드(Next.js) 포트 
  type: LoadBalancer
